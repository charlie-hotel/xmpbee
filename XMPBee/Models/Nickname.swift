// The nickname-sanitization String extension has been moved to Occupant.swift
// (which is already a member of the Xcode project target).  This file is left
// in place so its on-disk presence doesn't break anyone bookmarking the path,
// but it contains no symbols of its own.  Safe to delete from the working
// directory if you want — just don't add it to the project target afterward
// or you'll get a duplicate-definition link error.
import Foundation
